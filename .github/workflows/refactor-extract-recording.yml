name: extract-recording-once

on:
  push:
    branches: [refactor/modularize-app]
    paths:
      - scripts/refactor-extract-recording.mjs
      - scripts/refactor-recording-trigger.txt

permissions:
  contents: write

jobs:
  refactor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/modularize-app
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Extract recording controller
        run: node scripts/refactor-extract-recording.mjs
      - name: Commit generated refactor
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src
          git commit -m "refactor: extract recording controller" || exit 0
          git push origin HEAD:refactor/modularize-app
      - name: Build
        run: npm run build
      - name: Text tests
        run: npm run test:text
